(function(){
  window.settingsView=function(){const r=state.rules,key=!!kobotradeV20GetApiKey();return `<div class="subtitle">◼️ 買いルール</div><div class="card"><div class="setting-row" data-setting="downPct"><span class="setting-label">下落購入開始</span><span class="setting-value">−${r.downPct}% ›</span></div><div class="setting-row" data-setting="downContinue"><span class="setting-label">下落購入継続</span><span class="setting-value ${r.downContinue?'on':''}">${r.downContinue?'ON':'OFF'} ›</span></div><div class="setting-row" data-setting="downContinuePct"><span class="setting-label">下落継続率</span><span class="setting-value">−${r.downContinuePct}% ›</span></div><div class="setting-row" data-setting="upPct"><span class="setting-label">上昇購入開始</span><span class="setting-value">＋${r.upPct}% ›</span></div><div class="setting-row" data-setting="continue"><span class="setting-label">上昇購入継続</span><span class="setting-value ${r.continue?'on':''}">${r.continue?'ON':'OFF'} ›</span></div><div class="setting-row" data-setting="continuePct"><span class="setting-label">上昇継続率</span><span class="setting-value">＋${r.continuePct}% ›</span></div></div><div class="subtitle">◼️ スマホ通知</div><div class="card"><div class="setting-row" data-setting="notify"><span class="setting-label">通知</span><span class="setting-value ${r.notify?'on':''}">${r.notify?'ON':'OFF'} ›</span></div></div><div class="subtitle">◼️ 株価API</div><div class="card"><div class="row"><b>Twelve Data API Key</b></div><div class="row small" id="apiKeyState">API Key：${key?'保存済み（このiPhone）':'未設定'}</div><p class="small">API KeyはこのKoBoTradeの保存領域に保持します。同じホーム画面版を使い続ける限り通常は再入力不要です。</p><input id="apiKeyInput" type="password" autocomplete="off" placeholder="Twelve Data API Key"><button class="primary" id="saveApiKey">API Keyを保存</button><button class="secondary" id="deleteApiKey">API Keyを削除</button></div>`};
  window.bindSettings=function(){document.querySelectorAll("[data-setting]").forEach(el=>el.onclick=function(){const k=el.dataset.setting;if(["downContinue","continue","notify"].includes(k)){openModal(`<h2>${k==="continue"?"上昇購入継続":k==="downContinue"?"下落購入継続":"通知"}</h2><input id="boolSetting" type="checkbox" ${state.rules[k]?"checked":""}><button class="primary" id="saveBool">保存</button>`);document.getElementById("saveBool").onclick=function(){state.rules[k]=document.getElementById("boolSetting").checked;save();closeModal();render()}}else{const labels={downPct:"下落購入開始",downContinuePct:"下落継続率",upPct:"上昇購入開始",continuePct:"上昇継続率"};openModal(`<h2>${labels[k]}</h2><label>数値</label><input id="numSetting" type="number" min="0.01" step="0.01" value="${state.rules[k]}"><button class="primary" id="saveNum">保存</button>`);document.getElementById("saveNum").onclick=function(){const v=Number(document.getElementById("numSetting").value);if(v<=0){alert("0より大きい数値を入力してください");return}state.rules[k]=v;save();closeModal();render()}}});const s=document.getElementById("saveApiKey");if(s)s.onclick=function(){try{kobotradeV20SaveApiKey(document.getElementById("apiKeyInput").value);document.getElementById("apiKeyInput").value="";document.getElementById("apiKeyState").textContent="API Key：保存済み（このiPhone）";alert("API Keyを保存しました。")}catch(e){alert(e.message)}};const d=document.getElementById("deleteApiKey");if(d)d.onclick=function(){kobotradeV20ClearApiKey();document.getElementById("apiKeyState").textContent="API Key：未設定";alert("API Keyを削除しました。")};};
})();
